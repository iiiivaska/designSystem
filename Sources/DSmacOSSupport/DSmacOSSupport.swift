// DSmacOSSupport.swift
// DesignSystem
//
// macOS-specific integrations: hover styles, focus ring, toolbar, two-column forms.

import SwiftUI
import DSCore
import DSTheme
import DSControls
import DSForms
import DSSettings

/// DSmacOSSupport module namespace
public enum DSmacOSSupport {
    /// Current version of the macOS support module
    public static let version = "0.1.0"
}

#if os(macOS)
// macOS-specific implementations will be added here
#endif
